**What & why**
Short description of the change and the problem it solves.

**Checklist**
- [ ] `npm run build` passes (no TypeScript errors)
- [ ] No secrets / `.env*` / `~/.tokonomix/` artifacts committed
- [ ] Updated `skill/tokonomix-consensus/CHANGELOG.md` if user-visible
- [ ] Change is focused (one concern)
